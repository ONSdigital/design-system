---
title: Mutually exclusive
---
{% from "views/partials/example/_macro.njk" import patternlibExample %}
{% from "components/panel/_macro.njk" import onsPanel %}
{% from "components/external-link/_macro.njk" import onsExternalLink %}
{% from "components/lists/_macro.njk" import onsList %}

## When to use this component

The mutually exclusive component provides the user with a with a way of selecting multiple items or a single opposing value.

## How to use this component

This component degrades gracefully if JavaScript is not available to the user. If JavaScript is unavailable it is possible for the user to select all options without inverting the selection therefore further, server-side validation should be performed.

Inputs, radios, checkboxes, date inputs, and textareas all call this macro automatically if they have their `mutuallyExclusive` paramater set. This will add the necessary checkbox and aria-live markup, and the classes required by the JavaScript that runs the component. Where you are combining input components such as in the [duration example](#duration) you must call this macro and provide the class `js-exclusive-group-item` to the inputs.

## Examples

### Checkboxes

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/checkboxes/index.njk"})
}}

### Date

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/date-input/index.njk"})
}}

### Textarea

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/textarea/index.njk"})
}}

### Email

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/email/index.njk"})
}}

### Number/Currency

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/number/index.njk"})
}}

### Duration

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/duration/index.njk"})
}}

## How to check mutually exclusive answers
To help users select an answer, you should:
{{
    onsList({
        "itemsList": [
            {
                "text": 'check they have selected at least one checkbox'
            },
            {
                "text": 'show an error message if they have not selected any checkbox, including the mutually exclusive checkbox'
            },
            {
                "text": 'show an error message if they have not entered something in any of the fields or selected the mutually exclusive checkbox'
            },
            {
                "text": 'show an error message if what has been entered in an input field is not valid'
            }
        ]
    })
}}

### Error messages

Use the [correct errors pattern](/patterns/error-validation) and show the error details above the fieldset.

{{
    patternlibExample({"path": "components/mutually-exclusive/examples/date-input-error/index.njk"})
}}

Error validation for this component varies according to the mutually exclusive answer type used.

#### Use “Select [whatever it is]”.
For example, “Select what type of central heating you have”.

#### If all fields are empty and the mutually exclusive checkbox is not selected
Use “Enter [whatever the field asks for] or select the other option”.

For example, “Enter your feedback or select the other option”.

{% call onsPanel({
    "classes": "u-mb-s"
}) %}
    Each pattern and component that uses the mutually exclusive component has guidance on writing error messages: <a href="/patterns/access-codes">access codes pattern</a>, <a href="/patterns/addresses">addresses pattern</a>, <a href="/patterns/dates">dates pattern</a>, <a href="/patterns/durations">durations pattern</a>, <a href="/patterns/email-addresses">email addresses pattern</a>, <a href="/patterns/numeric-values">numeric values pattern</a>, <a href="/patterns/phone-numbers">phone numbers pattern</a>, <a href="/components/textarea">textarea component</a>.
{% endcall %}

## Help improve this component
Let us know how we could improve this component or share your user research findings.
{{
    onsExternalLink({
        "url": "https://github.com/ONSdigital/design-system/discussions/29",
        "linkText": "Discuss the ‘Mutually Exclusive’ component on GitHub"
    })
}}
