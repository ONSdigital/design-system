---
title: Error
---

+++
{% from "views/partials/example/_macro.njk" import patternlibExample %}
{% from "components/external-link/_macro.njk" import onsExternalLink %}
+++

The error component is used to wrap form inputs and fieldsets to to show validation errors.

{{
    patternlibExample({"path": "components/duration/examples/duration-error/index.njk"})
}}

There is specific guidance on [how to correct errors](/patterns/correct-errors).

## How to use this component

You should not typically need to “call” `onsError()` yourself as it is already called from within form input components. 

However, if you need to create a pattern or component that requires a error you can use {{
    onsExternalLink({
        "url": "https://mozilla.github.io/nunjucks/templating.html#call",
        "linkText": "nunjucks' call functionality"
    })
}}

| Name       | Type    | Required | Description                                                                                                                               |
| ---------- | ------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| text       | string  | true     | The text describing the error                                                                                                             |
| id         | string  | true     | The HTML `id` attribute that the [error summary](/patterns/correct-errors/#error-summary) link can anchor to                              |
| attributes | object  | false    | HTML attributes (for example, data attributes) to add to the field                                                                        |
| dsExample  | boolean | false    | Set to `true` if using component as an example in the Design System. Prevents focus on error on page load. *Only for use in DS examples*  |

## Help improve this component
Let us know how we could improve this component or share your user research findings.
{{
    onsExternalLink({
        "url": "https://github.com/ONSdigital/design-system/discussions/1379",
        "linkText": "Discuss the ‘Error’ component on GitHub"
    })
}}
