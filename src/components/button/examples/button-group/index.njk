{% from "components/button/_macro.njk" import onsButton %}
<div class="ons-btn-group">
    {{
        onsButton({
            "type": 'button',
            "text": 'Continue',
            "type": ['group']
        })
    }}

    {{
        onsButton({
            "type": 'button',
            "text": 'Cancel',
            "type": ['secondary', 'group']
        })
    }}
</div>
