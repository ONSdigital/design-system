{% from "components/icons/_macro.njk" import onsIcon %}

{% macro onsExternalLink(params) %}
    <span>
        <a href="{{- params.url -}}" class="external-link" target="_blank" rel="noopener">{{- params.linkText -}}{{- onsIcon({"icon": "external-link"})-}}</a>
    </span>
{% endmacro %}
