{%- macro onsFooterWarning(params) -%}
    {% from "components/panel/_macro.njk" import onsPanel %}
    <div class="footer-warning">
        <div class="container">
            <div class="grid">
                <div class="grid__col">
                    {% call onsPanel({
                            "type": "warn",
                            "classes": "panel--warn--footer"
                        })
                    %}
                        {{ caller() if caller }}
                    {% endcall %}
                </div>
            </div>
        </div>
    </div>
{%- endmacro -%}
