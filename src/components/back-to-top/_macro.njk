{% from "components/icon/_macro.njk" import onsIcon %}

{% macro onsBackToTop(params) %}
    <div class="ons-back-to-top ons-js-btt ons-back-to-top__enabled">
            <a href="#{{ params.anchor | default('top') }}" class="ons-back-to-top__link">
                {{
                    onsIcon({
                        "iconType": 'arrow-up',
                        "iconSize": 'xs'
                    })
                }}
                <span>{{ params.description | default('Back to top') }}</span>
            </a>
    </div>
{% endmacro %}
