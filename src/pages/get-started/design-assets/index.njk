---
title: Design assets
sortOrder: 2
---
{% from "components/lists/_macro.njk" import onsList %}
{% from "components/panel/_macro.njk" import onsPanel %}
{% from "components/external-link/_macro.njk" import onsExternalLink %}

## Sketch

Sketch is an application used to mock up wireframes and click through prototypes. 

{{
    onsExternalLink({
        "url": 'https://www.sketchapp.com/get/',
        "linkText": 'Download Sketch here'
    })
}}

### Add the ONS Sketch Library to Sketch

{{
    onsList({
        "element": 'ol',
        "itemsList": [
            {
                "url":'https://github.com/ONSdigital/ONS-Sketch-Library/raw/master/ONS-Sketch-Library.sketch', 
                "external": true,
                "text": 'Download the ONS Sketch Library'
            },
            {
                "text": 'Add the library to Sketch via <strong>Sketch > Preferences > Libraries</strong> by using the <strong>Add Library</strong> button.'
            },
            {
                "text": 'Access the symbols by clicking <strong>Insert</strong> and navigating to the ONS-Sketch-Library, under <strong>Symbols</strong>.'
            }
        ]
    })
}}

<br>

{% call onsPanel() %}
    If you require any help, notice any issues or have any recommendations please
    {{
        onsExternalLink({
            "url": "https://github.com/ONSdigital/ONS-Sketch-Library/issues",
            "linkText": "raise an issue on Github"
        })
    }}
{% endcall %}
