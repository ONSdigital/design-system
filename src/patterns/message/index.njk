---
title: Have conversations
group: Help users to...
---
{% from "views/partials/example/_macro.njk" import patternlibExample %}
## When to use this pattern

Use this pattern as a way to display conversations/threads between internal and external users of a service.

## How to use this pattern

This pattern consists of the following elements/components:
{% md %}
- Heading that displays the subject of the message thread
- Metadata that provides information about the service or survey
- A message thread
- Form field (textarea) to create a message
- Submit button to send a message
- Optional close button to end the conversation
{% endmd %}
All components should be wrapped in a `section` with `role=region` and an `aria-label`. The value of the aria-label should be prefixed with `conversation:` e.g. 
`aria-label="Conversation: BRES 2017 clarification required for Bolts &amp; Ratchets Ltd"`.

The heading that displays the subject should contain a visually hidden span e.g. `<span class="u-vh">Subject: </span>`. This helps to provide context to screen reader users about the page.

Each message in a thread is a seperate `message` component. Set the `type` parameter as `sent` to display the sent message correctly. 
Set the `type` parameter as `received` to display the received message correctly.

## Message - conversation
Each message can be stacked to display the conversation/thread.
{{
    patternlibExample({"path": "patterns/message/examples/conversation/index.njk"})
}}

## Research on this component
{% from "components/panel/_macro.njk" import onsPanel %}
{% call onsPanel() %}
   If you have conducted any user research using this component, please feed back your findings via the <a href="https://github.com/ONSdigital/design-system/issues/48" target="_blank">Design System forum</a>.
{% endcall %}
