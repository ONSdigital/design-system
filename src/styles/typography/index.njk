---
title: Typography
anchorLinks: true
---
{% from "styles/typography/lists/src/_macro.njk" import onsList %}
{% from "views/partials/example/_macro.njk" import patternlibExample %}

## Font
Text is the primary way that users digest information and accomplish tasks, so it’s important to use good typographic principles to establish a clear visual hierarchy and to maximize legibility.

### Lato
The primary font used in SDC is [Lato](https://fonts.google.com/specimen/Lato).

Lato, with its broad range of weights and extensive unicode support was chosen to provide a good balance between legibility, personality and readability. As the majority of our User Interface (UI) is text-based, we use weight and size to denote hierarchy and separation between content. Lato’s letterforms also provide a good level of contrast which aid readability on poorer quality displays and for users with visual impairment.

Font stack: <span style='font-family: Lato;'>"Lato"</span>, <span style='font-family: "Helvetica Neue";'>“Helvetica Neue”</span>, <span style='font-family: Helvetica;'>Helvetica</span>, <span style='font-family: Arial;'>Arial</span>, <span style='font-family: sans-serif;'>sans-serif</span>

### Type scale
Using the type scale classes on text elements, the type is sized appropriately for the following breakpoints:

{% md %}
| Viewport < 740                                            | Viewport >= 740                                           |
|-----------------------------------------------------------|-----------------------------------------------------------|
| <p class="u-fs-xl">u-fs-xl</p>Lato Bold - 1.5rem/28px     | <p class="u-fs-xl">u-fs-xl</p>Lato Bold - 2rem/36px       |
| <p class="u-fs-l">u-fs-l</p>Lato Bold - 1.3rem/24px       | <p class="u-fs-l">u-fs-l</p>Lato Bold - 1.5rem/28px       |
| <p class="u-fs-m">u-fs-m</p>Lato Bold - 1.1rem/20px       | <p class="u-fs-m">u-fs-m</p>Lato Bold - 1.2rem/22px       |
| <p class="u-fs-r--b">u-fs-r--b</p>Lato Bold - 1rem/18px   | <p class="u-fs-r--b">u-fs-r--b</p>Lato Bold - 1rem/18px   |
| <p class="u-fs-r">u-fs-r</p>Lato Regular - 1rem/18px      | <p class="u-fs-r">u-fs-r</p>Lato Regular - 1rem/18px      |
| <p class="u-fs-s--b">u-fs-s--b</p>Lato Bold - 0.7rem/14px | <p class="u-fs-s--b">u-fs-s--b</p>Lato Bold - 0.7rem/14px |
| <p class="u-fs-s">u-fs-s</p>Lato Regular - 0.7rem/14px    | <p class="u-fs-s">u-fs-s</p>Lato Regular - 0.7rem/14px    |
{% endmd %}

### Font weights
{{ 
    onsList({
        "classes": 'list--bare',
        "items": [
            {
                "text": '<b>Bold: 700</b>'
            },
            {
                "text": 'Regular: 400'
            }
        ]
    })
}}

## Headings
By default, headings have all their font-sizes reset to `1rem`, with the typographic hierarchy enforced by the 'type scale'.
{{
    patternlibExample({"path": "styles/typography/headings/examples/index.html"})
}}

### Breakpoint headings
<p>Font sizes can be triggered at certain breakpoints. Just suffix any font size class with <code>@{breakpoint}</code>, e.g. <code>u-fs-m@l</code></p>
<p class="u-fs-s@xs u-fs-r@s u-fs-m@l u-fs-l@xl u-fs-xl@xxl">This text will change font size depending on viewport width</p>


## Paragraphs
The base font size for paragraphs is 18px.

{{
    patternlibExample({"path": "styles/typography/paragraphs/examples/index.html"})
}}

## Lists
Use lists to break up blocks of text into concise, manageable chunks to make it easier to read.

### Bulleted list (default)
Bulleted lists should be introduced with a lead-in line, ending in a colon.

Each item in the list should start with a lowercase letter and should not end with a full stop.

{{
    patternlibExample({"path": "styles/typography/lists/examples/list/index.njk"})
}}

### Variants

#### Numbered list
Numbered lists should be displayed in place of bulleted lists when the order of items is relevant.

Each list item should be a complete sentence, therefore start with an uppercase letter, and end with a full stop.

You do not need to introduce a numbered list with a lead-in line.
{{
    patternlibExample({"path": "styles/typography/lists/examples/list-ordered/index.njk"})
}}

#### Bare list
Bare lists should be used to display a list of navigational links, for example, a group of related pages displayed in the [footer](components/footer).
{{
    patternlibExample({"path": "styles/typography/lists/examples/list-bare/index.njk"})
}}

#### Inline list
Inline lists should be used to display a horizontal row of links, for example, a list of pages displayed in the [transactional footer](components/footer#transactional).
{{
    patternlibExample({"path": "styles/typography/lists/examples/list-inline/index.njk"})
}}

#### Errors list
Used within the [error details panel](/patterns/error-validation#error-details) to display a list error messages for each erroneous field in a fieldset.
{{
    patternlibExample({"path": "styles/typography/lists/examples/list-errors/index.njk"})
}}

#### Boxed list
Used to display a code split into manageable chunks, for example, a reference code to quote during a helpdesk call.
{{
    patternlibExample({"path": "styles/typography/lists/examples/list-boxes/index.njk"})
}}

## Icons
Icons are used to provide affordance to improve user experience. Icons shouldn't be used as decoration and implemented only if there is a user need. We only use icons with text if we have evidence to suggest that it helps the user complete their task.

__Our current icon set can be used in the following scenarios:__

### List with progress check icons
{{
    patternlibExample({"path": "styles/typography/icons/examples/icons-list/index.njk"})
}}
__How to use:__ Use `icon--check` class on each `<li class="list__item">` within `<ul class="list list--bare">`.

### Button with print icon
{{
    patternlibExample({"path": "styles/typography/icons/examples/icons-button/index.njk"})
}}
__How to use:__ Use `icon--print-link` class on a `<button class="btn--secondary">`.

### Link with external link icon
{{
    patternlibExample({"path": "styles/typography/icons/examples/icons-link/index.html"})
}}
__How to use:__ Use `icon--external-link` class on an `<a>` element.

### Text with secure icon
{{
    patternlibExample({"path": "styles/typography/icons/examples/icons-text/index.html"})
}}
__How to use:__ Use `icon--lock` class on a `<p>` element.

### Contribute to icons
{% from "components/panel/src/_macro.njk" import onsPanel %}
{% call onsPanel() %}
   If you have any requests for icons or research findings please feed back via the <a href="https://github.com/ONSdigital/pattern-library-v2/issues/95" target="_blank">Design System forum</a>.
{% endcall %}
