<div class="grid grid--flex grid--gutterless">
    {% for item in (params.itemsList if params.itemsList is iterable else params.itemsList.items()) -%}
        <div class="patternlib-swatch grid grid--flex grid--gutterless grid--no-wrap">
            <div class="patternlib-swatch__color u-flex-no-shrink" style="background-color:{{ item.bg }}; {% if item.border -%} border:1px solid #ccc;{% endif -%}">
            {% if item.char -%}<span class="patternlib-swatch__char" style="color:{{ item.fore }}">{{ item.char }}</span>{% endif -%}
            </div>
            <div class="patternlib-swatch__codes u-flex-no-grow">
                {% if item.var -%}
                    <span class="patternlib-swatch__hex">{{ item.bg }}</span>
                    <code class="patternlib-swatch__var">{{ item.var }}</code>
                {% endif -%}

                {% if item.fore -%}
                    <span class="patternlib-swatch__detail">Foreground: <strong>{{ item.fore }}</strong></span>
                    <span class="patternlib-swatch__detail">Background: <strong>{{ item.bg }}</strong></span>
                {% endif -%}
            </div>
            {% if item.info -%}
            <div class="patternlib-swatch__info grid__col u-flex-shrink">{{ item.info|safe  }}</div>
            {% endif -%}
        </div>
    {% endfor -%}
</div>
